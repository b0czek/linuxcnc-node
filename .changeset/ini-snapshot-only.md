---
"@linuxcnc-node/grpc-client": minor
---

Replace per-key INI lookup and conversion RPCs with IniService.Read, returning the complete immutable parsed snapshot with repeated entries and resolved includes.

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Dariusz Majnert <dariusz.majnert@gmail.com>
Date: Wed, 2 Sep 2026 15:31:24 +0200
Subject: [PATCH] fix(spindle): publish modal CSS maximum in status

---
 src/emc/task/emccanon.cc                      |  4 +-
 src/emc/task/emctaskmain.cc                   |  2 +-
 src/emc/task/taskintf.cc                      |  2 +-
 src/emc/usr_intf/axis/extensions/emcmodule.cc |  1 +
 tests/css-status/README                       |  6 ++
 tests/css-status/checkresult                  |  4 ++
 tests/css-status/core_sim.hal                 | 10 +++
 tests/css-status/test-ui.py                   | 61 +++++++++++++++++++
 tests/css-status/test.ini                     | 55 +++++++++++++++++
 tests/css-status/test.sh                      |  4 ++
 tests/css-status/tool.tbl                     |  0
 11 files changed, 146 insertions(+), 3 deletions(-)
 create mode 100644 tests/css-status/README
 create mode 100755 tests/css-status/checkresult
 create mode 100644 tests/css-status/core_sim.hal
 create mode 100755 tests/css-status/test-ui.py
 create mode 100644 tests/css-status/test.ini
 create mode 100755 tests/css-status/test.sh
 create mode 100644 tests/css-status/tool.tbl

diff --git a/src/emc/task/emccanon.cc b/src/emc/task/emccanon.cc
index ad1352a7d4..9f36a8de8e 100644
--- a/src/emc/task/emccanon.cc
+++ b/src/emc/task/emccanon.cc
@@ -3027,7 +3027,9 @@ void SPINDLE_RETRACT_TRAVERSE()
 }
 
 void SET_SPINDLE_MODE(int spindle, double css_max) {
-   canon.spindle[spindle].css_maximum = fabs(css_max);
+    canon.spindle[spindle].css_maximum = fabs(css_max);
+    emcStatus->motion.spindle[spindle].css_maximum =
+        canon.spindle[spindle].css_maximum;
 }
 
 template <class MSG=EMC_SPINDLE_ON>
diff --git a/src/emc/task/emctaskmain.cc b/src/emc/task/emctaskmain.cc
index a981031dbc..1f30d33f4f 100644
--- a/src/emc/task/emctaskmain.cc
+++ b/src/emc/task/emctaskmain.cc
@@ -919,7 +919,7 @@ static void emcTaskStopProgress()
         bool spindle_output_active = false;
         for (int s = 0; s < emcStatus->motion.traj.spindles; ++s) {
             const auto &status = emcStatus->motion.spindle[s];
-            bool spindle_running = status.enabled || status.direction != 0;
+            bool spindle_running = status.enabled;
             if (!stop_outputs_saved) {
                 stop_spindles[s].running = spindle_running;
                 stop_spindles[s].speed = status.speed;
diff --git a/src/emc/task/taskintf.cc b/src/emc/task/taskintf.cc
index 4796832331..d88c727d52 100644
--- a/src/emc/task/taskintf.cc
+++ b/src/emc/task/taskintf.cc
@@ -2046,7 +2046,7 @@ int emcSpindleUpdate(EMC_SPINDLE_STAT stat[], int num_spindles){
 
     for (s = 0; s < num_spindles; s++){
 		stat[s].spindle_override_enabled = enables & SS_ENABLED;
-		stat[s].enabled = emcmotStatus.spindle_status[s].speed != 0;
+		stat[s].enabled = emcmotStatus.spindle_status[s].state != 0;
 		stat[s].speed = emcmotStatus.spindle_status[s].speed;
 		stat[s].feedback = emcmotStatus.spindle_status[s].spindleSpeedIn * 60.0;
 		stat[s].brake = emcmotStatus.spindle_status[s].brake;
diff --git a/src/emc/usr_intf/axis/extensions/emcmodule.cc b/src/emc/usr_intf/axis/extensions/emcmodule.cc
index 1e207b3afb..dd93300714 100644
--- a/src/emc/usr_intf/axis/extensions/emcmodule.cc
+++ b/src/emc/usr_intf/axis/extensions/emcmodule.cc
@@ -1458,6 +1458,7 @@ static PyObject *Stat_spindle_one(pyStatChannel *s, int spindleno) {
     F2("override_enabled", spindle_override_enabled);
     F(speed);
     F(feedback);
+    F2("css_maximum", css_maximum);
     F2("override", spindle_scale);
     F(homed);
     F(orient_state);
diff --git a/tests/css-status/README b/tests/css-status/README
new file mode 100644
index 0000000000..66ad330dd7
--- /dev/null
+++ b/tests/css-status/README
@@ -0,0 +1,6 @@
+Verify CSS maximum spindle speed status follows the active G96/G97 mode.
+
+The regression enters G96 while the spindle is stopped, checks that the D
+word is published through spindle status, verifies M5 does not discard that
+modal limit, and checks that G97 clears it. It also verifies that spindle
+`enabled` follows the explicit on/off state rather than a nonzero CSS limit.
diff --git a/tests/css-status/checkresult b/tests/css-status/checkresult
new file mode 100755
index 0000000000..56f319c1fe
--- /dev/null
+++ b/tests/css-status/checkresult
@@ -0,0 +1,4 @@
+#!/bin/sh
+set -e
+
+grep -q '^CSS status scenario ok$' "$1"
diff --git a/tests/css-status/core_sim.hal b/tests/css-status/core_sim.hal
new file mode 100644
index 0000000000..e244f001df
--- /dev/null
+++ b/tests/css-status/core_sim.hal
@@ -0,0 +1,10 @@
+loadrt trivkins
+loadrt motmod base_period_nsec=0 servo_period_nsec=1000000 num_joints=1 num_spindles=1
+
+addf motion-command-handler servo-thread
+addf motion-controller servo-thread
+
+net Xpos joint.0.motor-pos-cmd => joint.0.motor-pos-fb
+net estop-loop iocontrol.0.user-enable-out iocontrol.0.emc-enable-in
+
+setp spindle.0.at-speed true
diff --git a/tests/css-status/test-ui.py b/tests/css-status/test-ui.py
new file mode 100755
index 0000000000..2372f6c89e
--- /dev/null
+++ b/tests/css-status/test-ui.py
@@ -0,0 +1,61 @@
+#!/usr/bin/env python3
+
+import math
+import sys
+import time
+
+import linuxcnc
+
+
+def fail(message):
+    print(message)
+    sys.exit(1)
+
+
+def wait_complete(message, timeout=10.0):
+    if command.wait_complete(timeout) != linuxcnc.RCS_DONE:
+        fail(message)
+    status.poll()
+
+
+def assert_css_maximum(expected, message):
+    status.poll()
+    actual = status.spindle[0]["css_maximum"]
+    if not math.isclose(actual, expected, abs_tol=0.001):
+        fail("%s: expected %.3f, got %.3f" % (message, expected, actual))
+
+
+command = linuxcnc.command()
+status = linuxcnc.stat()
+
+deadline = time.time() + 10.0
+while time.time() < deadline:
+    status.poll()
+    if status.task_state == linuxcnc.STATE_ESTOP:
+        break
+    time.sleep(0.02)
+else:
+    fail("LinuxCNC did not initialize")
+
+command.state(linuxcnc.STATE_ESTOP_RESET)
+command.state(linuxcnc.STATE_ON)
+wait_complete("machine did not turn on")
+
+command.mode(linuxcnc.MODE_MDI)
+wait_complete("machine did not enter MDI")
+
+command.mdi("G96 S100 D2111")
+wait_complete("G96 command did not complete")
+assert_css_maximum(2111.0, "G96 did not publish its D word")
+if status.spindle[0]["enabled"]:
+    fail("G96 reported a stopped spindle as running")
+
+command.mdi("M5")
+wait_complete("M5 command did not complete")
+assert_css_maximum(2111.0, "M5 discarded the modal CSS maximum")
+
+command.mdi("G97")
+wait_complete("G97 command did not complete")
+assert_css_maximum(0.0, "G97 did not clear the CSS maximum")
+
+print("CSS status scenario ok")
diff --git a/tests/css-status/test.ini b/tests/css-status/test.ini
new file mode 100644
index 0000000000..61ccf6bc64
--- /dev/null
+++ b/tests/css-status/test.ini
@@ -0,0 +1,55 @@
+[EMC]
+VERSION = 1.1
+DEBUG = 0x0
+
+[DISPLAY]
+DISPLAY = ./test-ui.py
+
+[RS274NGC]
+PARAMETER_FILE = sim.var
+
+[EMCMOT]
+EMCMOT = motmod
+COMM_TIMEOUT = 4.0
+BASE_PERIOD = 0
+SERVO_PERIOD = 1000000
+
+[TASK]
+TASK = milltask
+CYCLE_TIME = 0.001
+
+[HAL]
+HALFILE = core_sim.hal
+
+[TRAJ]
+NO_FORCE_HOMING = 1
+AXES = 1
+COORDINATES = X
+SPINDLES = 1
+LINEAR_UNITS = mm
+ANGULAR_UNITS = degree
+DEFAULT_LINEAR_VELOCITY = 10
+MAX_LINEAR_VELOCITY = 50
+
+[EMCIO]
+TOOL_TABLE = tool.tbl
+
+[KINS]
+KINEMATICS = trivkins coordinates=X
+JOINTS = 1
+
+[AXIS_X]
+MIN_LIMIT = -100
+MAX_LIMIT = 100
+MAX_VELOCITY = 50
+MAX_ACCELERATION = 500
+
+[JOINT_0]
+TYPE = LINEAR
+HOME = 0
+MAX_VELOCITY = 50
+MAX_ACCELERATION = 500
+MIN_LIMIT = -100
+MAX_LIMIT = 100
+FERROR = 1
+MIN_FERROR = 0.25
diff --git a/tests/css-status/test.sh b/tests/css-status/test.sh
new file mode 100755
index 0000000000..13436c16b1
--- /dev/null
+++ b/tests/css-status/test.sh
@@ -0,0 +1,4 @@
+#!/bin/sh
+set -e
+
+linuxcnc -r test.ini
diff --git a/tests/css-status/tool.tbl b/tests/css-status/tool.tbl
new file mode 100644
index 0000000000..e69de29bb2
